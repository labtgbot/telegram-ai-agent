# Docker

Файлы для контейнеризации:

- `Dockerfile.backend`
- `Dockerfile.mini-app`
- `Dockerfile.admin`
- `compose.yml` — локальный стек (backend + postgres + redis + minio)
- `compose.prod.yml` — production-like стек

> ⚠️ Реализация будет добавлена в Phase 1 / Phase 4 (см. соответствующие issues).
