apiVersion: v1
kind: ServiceAccount
metadata:
  name: telegram-ai-agent-backup
  namespace: tgai-backup
  labels:
    app.kubernetes.io/part-of: telegram-ai-agent
    app.kubernetes.io/component: backup
  annotations:
    # EKS / IRSA — uncomment and set when running on AWS:
    # eks.amazonaws.com/role-arn: arn:aws:iam::<account>:role/tgai-backup
    {}
