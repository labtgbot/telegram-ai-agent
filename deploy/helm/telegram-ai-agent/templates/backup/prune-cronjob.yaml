{{- if and .Values.backup.enabled .Values.backup.prune.enabled }}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "telegram-ai-agent.componentName" (dict "ctx" . "name" "backup-prune") }}
  labels:
    {{- include "telegram-ai-agent.componentLabels" (dict "ctx" . "name" "backup") | nindent 4 }}
spec:
  schedule: {{ .Values.backup.prune.schedule | quote }}
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  startingDeadlineSeconds: 3600
  jobTemplate:
    spec:
      backoffLimit: 1
      ttlSecondsAfterFinished: 86400
      template:
        metadata:
          labels:
            {{- include "telegram-ai-agent.componentSelectorLabels" (dict "ctx" . "name" "backup") | nindent 12 }}
            backup.tgai/job: prune
        spec:
          {{- include "telegram-ai-agent.backup.podSpec" . | nindent 10 }}
          containers:
            - name: prune
              image: {{ include "telegram-ai-agent.backup.image" . }}
              imagePullPolicy: {{ .Values.image.pullPolicy }}
              command: ["/opt/tgai/backup/prune-backups.sh"]
              envFrom:
                {{- include "telegram-ai-agent.backup.envFrom" . | nindent 16 }}
              resources:
                {{- toYaml .Values.backup.resources | nindent 16 }}
              securityContext:
                allowPrivilegeEscalation: false
                readOnlyRootFilesystem: true
                capabilities:
                  drop: ["ALL"]
              volumeMounts:
                - name: tmp
                  mountPath: /tmp
                - name: aws-cache
                  mountPath: /home/nobody/.aws/cli
          volumes:
            - name: tmp
              emptyDir: {}
            - name: aws-cache
              emptyDir: {}
{{- end }}
