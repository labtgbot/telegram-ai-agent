{{- if .Values.admin.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "telegram-ai-agent.componentName" (dict "ctx" . "name" "admin") }}
  labels:
    {{- include "telegram-ai-agent.componentLabels" (dict "ctx" . "name" "admin") | nindent 4 }}
spec:
  type: {{ .Values.admin.service.type }}
  ports:
    - name: http
      port: {{ .Values.admin.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "telegram-ai-agent.componentSelectorLabels" (dict "ctx" . "name" "admin") | nindent 4 }}
{{- end }}
